name: Project Test

on:
  pull_request:
    branches:
      - 'dev/*'
      - '**enterprise**'

jobs:
  set_version:
    runs-on: ubuntu-latest
    outputs:
      emqx-name: ${{ steps.set-output.outputs.emqx-name }}
    steps:
    - name: set output
      id: set-output
      run: |
        echo "emqx-name=emqx-enterprise" >> "$GITHUB_OUTPUT"

  build_emqx_for_test:
    needs:
      - set_version
    uses: ./.github/workflows/build_emqx_for_test.yaml
    with:
      emqx-name: ${{ needs.set_version.outputs.emqx-name }}

  # dashboard_test:
  #   needs:
  #     - build_emqx_for_test
  #   uses: ./.github/workflows/dashboard_test.yaml

  check_schema:
    needs:
      - set_version
      - build_emqx_for_test
    uses: ./.github/workflows/check_newest_schema.yaml
    with:
      emqx-name: ${{ needs.set_version.outputs.emqx-name }}

  check_audit_dict:
    needs:
      - set_version
      - build_emqx_for_test
    uses: ./.github/workflows/check_audit_dict.yaml
    with:
      emqx-name: ${{ needs.set_version.outputs.emqx-name }}
