name: Dashboard Test

on:
  pull_request:
  push:
    branches:
      - 'dev/*'
      - 'enterprise'

jobs:
  build_emqx_for_test:
    uses: ./.github/workflows/build_emqx_for_test.yaml
   
  # test:


  check_schema:
    needs:
      - build_emqx_for_test
    uses: ./.github/workflows/check_newest_schema.yaml

